# Hardware-specific dependencies for Raspberry Pi
# These may fail to install on non-RPi platforms (x86/x64).
# Install with: pip install -r requirements-hw.txt || true
gpiozero>=2.0,<3.0
RPi.GPIO>=0.7,<1.0
spidev>=3.6,<4.0
mfrc522>=0.0.7
rpi-ws281x>=5.0,<6.0
smbus2>=0.4,<1.0
